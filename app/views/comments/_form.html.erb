<%= form_with(model: [content, comment], local: true) do |form| %>
  <div class="field">
    <%= form.label :body %>
    <%= form.text_area(:body, required: true) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

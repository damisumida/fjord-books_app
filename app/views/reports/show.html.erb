<h1>Report</h1>

<p>
  <strong>Title:</strong>
  <%= @report.title %>
</p>

<p>
  <strong>Body Text:</strong>
  <%= @report.body_text %>
</p>

<%= link_to 'Edit', edit_report_path(@report) %> |
<%= link_to 'Back', reports_path %>

<%= render 'comments/index' %>
<%= render 'comments/form', content: @report, comment: @comment %>
